#Capacitor needs to be able to access these.
Header add Access-Control-Allow-Origin "*"

#Stop disk cache from blocking updates (6 hour max-age)
<FilesMatch "\.(css|js)$">
Header set Cache-Control "max-age=21600, public, must-revalidate"
</FilesMatch>
